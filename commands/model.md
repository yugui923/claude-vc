---
description: Generate a 3-statement financial model
argument-hint: "[--no-docx] [--no-xlsx]"
---

Load the `vc-model` skill and generate a financial model.
Exports DOCX and XLSX by default; use `--no-docx` / `--no-xlsx` to skip.
Pass all arguments through to the skill.
