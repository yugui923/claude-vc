---
description: Calculate fund return metrics (IRR, MOIC, DPI, TVPI, PME)
argument-hint: "[investment data, CSV, or file path] [--no-xlsx]"
---

Load the `vc-returns` skill and calculate return metrics.
Exports XLSX by default; use `--no-xlsx` to skip.
Pass all arguments through to the skill.
